first_thing:
- target: rails
  execute: true
  apply_during: all
  sudo: true
  inline: |
    #!/usr/bin/env bash
    echo "Remove Vite Cache"

    rm -f /tmp/cache/vite

    echo "Vite Cache remove"
